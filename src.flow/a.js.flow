// @flow
export interface Pith<A> {
  (ray: (a: A) => void): void;
}
export interface Bark<A, B> {
  (pith: Pith<A>): B;
}
declare export default function tree<B, A>(
  deltaC: (as: Array<A>) => B
): Bark<A, B>

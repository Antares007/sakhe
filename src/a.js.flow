// @flow
export type Ray<A> = (a: A) => void

export type Pith<A> = (ray: Ray<A>) => void

export type Bark<A, B> = (pith: Pith<A>) => B

declare export default function tree<B, A>(
  deltaC: (as: Array<A>) => B
): Bark<A, B>
